---
collections:
  - name: community.general
    version: ">=8.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
  # IdM user/group/HBAC modules (freeipa.ansible_freeipa.*).
  # Also available as RPM ansible-freeipa on RHEL controllers.
  - name: freeipa.ansible_freeipa
    version: ">=1.12.0"
