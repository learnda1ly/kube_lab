# Persistent index for Mac endpoint logs (UF → S2S → this indexer).
[macOS]
homePath   = $SPLUNK_DB/macOS/db
coldPath   = $SPLUNK_DB/macOS/colddb
thawedPath = $SPLUNK_DB/macOS/thaweddb
maxTotalDataSizeMB = 10240
